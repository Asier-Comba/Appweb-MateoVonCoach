import React from "react";
import ChatShell from "@/components/chat/ChatShell";

/**
 * ChatPage
 * Nota: Horizons tiene límites de tamaño de archivo; por eso esta página es un wrapper.
 */
export default function ChatPage() {
  return <ChatShell />;
}